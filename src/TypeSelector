import React,{Component} form 'react';
import{
  AppRegistry,
  StyleSheet,
  Text,
  ListView,
} from 'react-native'
import * as firebase from 'firebase';
import NavigationBar from 'react-native-navigationbar';

export class StatusSelector extends  Compoent {
    constructor(props){
      super(props);
      this.state={
        
      }
    }



}
